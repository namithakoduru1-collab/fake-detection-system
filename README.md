# fake-detection-system
Face Detection System is a Python application that uses OpenCV to detect human faces in real time through a webcam. It uses a pre-trained Haar Cascade classifier to identify faces and highlight them with bounding boxes, demonstrating basic computer vision and image processing concepts.
